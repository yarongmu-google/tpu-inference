# Llama 3 8B on TPU v7x — balanced 1024 in / 1024 out.
#
# Mirrors bm-infra's hourly_tt_v7.csv workload (which uses
# Qwen3-Coder-30B but identical max_num_seqs / batched_tokens /
# model_len / dataset / lengths). Use this as the apples-to-apples
# perf benchmark vs the QC repo's standard methodology.
#
# All values use `: "${VAR:=default}"` so an env var set by the
# caller wins over the default here. That makes this case file
# reusable across sweeps that override individual axes.
#
# Two bash quirks worth knowing:
#   1. The colon-form `:=` triggers on unset OR empty, so passing
#      `VAR=""` from the caller silently picks up the default here.
#      To override, set a non-empty value.
#   2. The script (run_benchmark.sh) uses the `:-` form for its own
#      defaults — it substitutes a value without assigning. They look
#      similar; do not copy-paste between the two contexts blindly.

: "${MODEL:=meta-llama/Meta-Llama-3-8B}"
: "${TENSOR_PARALLEL_SIZE:=1}"

# Match bm-infra v7x defaults so workload shape is identical to the
# Qwen3-Coder run except for the model.
: "${MAX_NUM_SEQS:=128}"
: "${MAX_NUM_BATCHED_TOKENS:=10275}"
: "${MAX_MODEL_LEN:=10275}"

# vLLM's --block-size (KV-cache page size). Empty = let vLLM pick its
# default for this version (the value drifts across releases — check
# the vllm CLI or its --block-size doc for what your install uses).
# Set to 64 or 128 to match a tuned page_size from the kernel-tuning
# table.
: "${BLOCK_SIZE:=}"

: "${DATASET:=sonnet}"
: "${INPUT_LEN:=1024}"
: "${OUTPUT_LEN:=1024}"
: "${NUM_PROMPTS:=1000}"
